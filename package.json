{
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongoose": "^8.3.4",
    "sweetalert2": "^11.10.8"
  },
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "install:apps": "cd frontend && npm install && cd .. && cd backend && npm install && cd ..",
    "dev": "nodemon app.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
